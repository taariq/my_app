class MyApp.ExamplesController extends MyApp.ApplicationController
  routingKey: 'examples'
  # Add actions to this controller by defining functions on it.
  #
  # show: (params) ->

  index: ->

  # Add functions to run before an action with
  #
  # @beforeFilter 'someFunctionKey'  # or
  # @beforeFilter -> ...
  #
